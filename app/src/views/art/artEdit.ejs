<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        .message {
            background-color: red;
            color: #fff;
            padding: 5px 10px;
            visibility: hidden;
            font-weight: bold;
        }
        .hidden {
            visibility: hidden;
        }
        .btn { display: inline-block; 
	 padding: 4px 10px 4px; margin-bottom: 0; font-size: 13px; line-height: 18px; color: #333333; text-align: center;text-shadow: 0 1px 1px rgba(255, 255, 255, 0.75); vertical-align: middle; background-color: #f5f5f5; background-image: -moz-linear-gradient(top, #ffffff, #e6e6e6); background-image: -ms-linear-gradient(top, #ffffff, #e6e6e6); background-image: -webkit-gradient(linear, 0 0, 0 100%, from(#ffffff), to(#e6e6e6)); background-image: -webkit-linear-gradient(top, #ffffff, #e6e6e6); background-image: -o-linear-gradient(top, #ffffff, #e6e6e6); background-image: linear-gradient(top, #ffffff, #e6e6e6); background-repeat: repeat-x; filter: progid:dximagetransform.microsoft.gradient(startColorstr=#ffffff, endColorstr=#e6e6e6, GradientType=0); border-color: #e6e6e6 #e6e6e6 #e6e6e6; border-color: rgba(0, 0, 0, 0.1) rgba(0, 0, 0, 0.1) rgba(0, 0, 0, 0.25); border: 1px solid #e6e6e6; -webkit-border-radius: 4px; -moz-border-radius: 4px; border-radius: 4px; -webkit-box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.2), 0 1px 2px rgba(0, 0, 0, 0.05); -moz-box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.2), 0 1px 2px rgba(0, 0, 0, 0.05); box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.2), 0 1px 2px rgba(0, 0, 0, 0.05); cursor: pointer; }

    </style>
    
</head>
<body>
    <p class="message">Error</p>
    <form class="form" enctype="multipart/form-data">
        <p>작성자: <%=nickname%></p>
        <p id="postID" class="hidden"><%=data[0].postID%></p>
        <p><input id="title" name="title" type="text" placeholder="제목" value="<%=data[0].title%>"/></p>
        <p><input id="content" name="content" type="textarea" placeholder="내용을 입력해 주세요." value="<%=data[0].content%>"/></p>
        <p><input id="image" name="image" type="file"></p>
        <p><input id="instructorName" name="instructorName" type="text" placeholder="강사명" value="<%=data[0].instructorName%>"/></p>
        <p><select id="category" name="category">
            <option value="0">카테고리</option>
            <option value="humanities" <% if (data[0].category == 'humanities') { %> selected <% } %>>인문학</option>
            <option value="music" <% if (data[0].category == 'music') { %> selected <% } %>>음악</option>
            <option value="art" <% if (data[0].category == 'art') { %> selected <% } %>>미술</option>
            <option value="sports" <% if (data[0].category == 'sports') { %> selected <% } %>>운동</option>
            <option value="show" <% if (data[0].category == 'show') { %> selected <% } %>>공연</option>
        </select></p>
        <p><input id="eduPeriod" name="eduPeriod" type="text" placeholder="교육기간" value="<%=data[0].eduPeriod%>"/></p>
        <p><input id="recruitNum" name="recruitNum" type="text" placeholder="모집인원" value="<%=data[0].recruitNum%>"/></p>
        <p><input id="receptionPeriod" name="receptionPeriod" type="text" placeholder="접수기간" value="<%=data[0].receptionPeriod%>"/></p>
        <p><input id="place" name="place" type="text" placeholder="장소" value="<%=data[0].place%>"/></p>
        <p><select id="status" name="status">
            <option value="ing" <% if (data[0].status == 'ing') { %> selected <% } %>>접수중</option>
            <option value="end" <% if (data[0].status == 'end') { %> selected <% } %>>접수마감</option>
        </select></p>
        <p id="button" class="btn">수정하기</p>
    </form>
    <script src="/js/art/artedit.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
</body>
</html>