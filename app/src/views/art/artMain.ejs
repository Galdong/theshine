<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <table border="1">
        <tr>
            <th>제목</th>
            <th>내용</th>
            <th>작성자</th>
            <th>교육기간</th>
            <th>접수기간</th>
            <th>작성일</th>
            <th>상태</th>
        </tr>
        <% for (let i=(page*page_num) - page_num; i<(page*page_num); i++) { %>
            <% if (i > length) { %>
                <% i++; %>
            <% } else { %>
            <tr>
                <td><a href="/art/list/<%=page%>/<%=data[i].postID%>"><%=data[i].title%></a></td>
                <td><%=data[i].content%></td>
                <td><%=data[i].nickname%></td>
                <td><%=data[i].eduPeriod%></td>
                <td><%=data[i].receptionPeriod%></td>
                <td><%=data[i].postDate%></td>
                <td><% if (data[i].status == 'ing') { %> 접수중 <% } else if (data[i].status == 'end') { %> 접수마감 <% } %></td>
            </tr>
            <% } %>
        <% } %>
        <tr>
            <td colspan="7">
                <% for(let j=0; j<(length/page_num); j++) { %>
                [<a href="/art/list/<%=j+1%>"><%=j+1%></a>]
                <% } %>
            </td>
        </tr>
    </table>
    <a href="/art/write">글쓰기</a>
</body>
</html>